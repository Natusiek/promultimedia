import 'package:flutter/widgets.dart';

class Borders {
  //ANCHOR - Borders
  static const all50 = BorderRadius.all(Radius.circular(50));

  //ANCHOR - Shape
  static const shape50 = RoundedRectangleBorder(borderRadius: all50);
}
